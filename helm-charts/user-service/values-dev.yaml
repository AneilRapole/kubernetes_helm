name: user-service

replicaCount: 1

image:
  repository: europe-west2-docker.pkg.dev/new-dev-492605/myhomerepo/user-service
  tag: "1.0"

containerPort: 5000

service:
  type: ClusterIP
  port: 5000
  targetPort: 5000

resources:
  requests:
    cpu: "50m"
  limits:
    cpu: "150m"

env: dev

config:
  orderServiceUrl: "http://order-service.dev.svc.cluster.local"