apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: dev
spec:
  selector:
    app: user-deployment   # MUST match pod label exactly
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP