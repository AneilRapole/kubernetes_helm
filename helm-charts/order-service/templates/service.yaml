apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: dev
spec:
  selector:
    app: order-deployment   # MUST match pod label exactly
  ports:
    - port: 80
      targetPort: 5001
  type: ClusterIP